<?xml version="1.0" encoding="utf-8"?>
<!--
    Phase 8 \u00b7 Step 8.13 \u2014 SportStream Admin (com.streamify.admin).

    Two activities for now:
      \u2022 LoginActivity  \u2014 entry point, exported=true so admins can install + launch
      \u2022 DashboardActivity \u2014 post-auth landing

    Only INTERNET requested additional. Auth + biometrics land in Step 8.16
    (Android 6\u201317 compat layer).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".StreamifyAdminApp"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.StreamifyAdmin"
        tools:targetApi="31">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.StreamifyAdmin.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.StreamifyAdmin.NoActionBar" />

    </application>

</manifest>
