<?xml version="1.0" encoding="utf-8"?>
<!--
    Phase 5 · Step 5.5 — Bell icon for the Notice screen.

    Used in two places:
      • fragment_notice.xml FAB (`app:icon` + `app:iconTint=dark_bg`).
      • notice_empty_state ImageView (alpha=0.6 means tint is mostly
        cosmetic; we keep the fillColor=@android:color/white so the
        alpha treatment reads cleanly under both color modes).

    Viewport is 24x24 to match the Material Design 'notifications' icon
    so the host (ExtendedFloatingActionButton) sizes it the same as its
    built-in icons. Path is the standard bell outline + clapper; tint is
    applied by the host.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="@android:color/white"
        android:pathData="M12,22c1.1,0 2,-0.9 2,-2h-4c0,1.1 0.9,2 2,2zM18,16v-5c0,-3.07 -1.64,-5.64 -4.5,-6.32V4c0,-0.83 -0.67,-1.5 -1.5,-1.5s-1.5,0.67 -1.5,1.5v0.68C7.63,5.36 6,7.92 6,11v5l-2,2v1h16v-1l-2,-2z" />
</vector>
