<?xml version="1.0" encoding="utf-8"?>
<!--
    Phase 4 · Step 4.6 — Floating player overlay rounded background.

    Solid primary-tinted 16 dp rounded rect with a 1 dp primary-50 % stroke.
    Matches the Step 1.5 glass / card aesthetic so the overlay reads as
    part of the SportStream design system when floating over other apps.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/glass_primary" />
    <stroke
        android:width="1dp"
        android:color="@color/glass_primary_border" />
    <corners android:radius="16dp" />
</shape>
