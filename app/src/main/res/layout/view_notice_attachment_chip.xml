<?xml version="1.0" encoding="utf-8"?>
<!--
    Phase 5 · Step 5.5 v2 — Attachment chip (rendered inside
    NoticeAdapter's horizontal strip). Tap → open link via ACTION_VIEW.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="8dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:background="@drawable/bg_glass_card"
    android:paddingStart="10dp"
    android:paddingEnd="10dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:id="@+id/attachmentChipIcon"
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:contentDescription="@null"
        android:src="@drawable/ic_notice_section_info"
        tools:tint="@color/primary"
        xmlns:tools="http://schemas.android.com/tools" />

    <TextView
        android:id="@+id/attachmentChipLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="6dp"
        android:fontFamily="@font/poppins_regular"
        android:textColor="@color/primary"
        android:textSize="11sp"
        tools:text="Open link" />
</LinearLayout>
