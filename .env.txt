# =============================================================================
# Streamify Backend — Environment Variables for Render
# =============================================================================
# Rename this file from .env.txt to just .env before uploading to Render,
# or copy-paste each variable into Render's Environment Variables dashboard.
# =============================================================================

# ── Database (Render standard: DATABASE_URL connection string) ──
# Do NOT use DB_HOST, DB_USER, DB_PASSWORD, DB_NAME — Render blocks them.
DATABASE_URL=postgres://postgres.izpvcoikstplxcqzgsfh:lmtUaw5sn6hQeADR@aws-1-ap-south-1.pooler.supabase.com:6543/postgres

# ── JWT Secrets (cryptographically generated — keep these safe!) ──
JWT_SECRET=1bLJFTXeTwoKKN4RrCDVCW0lFq8MoAUGi2wnfDPr
JWT_EXPIRES_IN=7d
ADMIN_JWT_SECRET=fuTgxdBip12wmW3OUsq8PDqooLCY53wMTuNw8LTC
ADMIN_JWT_EXPIRES_IN=24h

# ── Server ──
NODE_ENV=production
# PORT is auto-assigned by Render — do NOT set it here

# ── Admin Seed (auto-creates super admin on first run) ──
ADMIN_SEED_EMAIL=admin@streamify.app
ADMIN_SEED_PASSWORD=admin123

# ── Firebase / FCM (push notifications) ──
# Uncomment and fill these when you have a Firebase project set up.
# Without them, FCM push notifications are silently disabled.
# FIREBASE_PROJECT_ID=your-firebase-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----
YOUR_KEY_HERE
-----END PRIVATE KEY-----


# FCM_DEFAULT_TOPIC=all_users
